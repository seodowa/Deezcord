# Layouts

This directory contains reusable page wrapper components or layouts (e.g., `MainLayout`, `AuthLayout`). These components define the common structure of pages, such as navigation bars, sidebars, and footers.
