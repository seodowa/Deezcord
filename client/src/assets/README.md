# Assets

This directory is for static assets that need to be imported and processed by Vite (the module bundler). This includes files like images, SVGs, global stylesheets (CSS/SCSS), or fonts that are referenced directly in the JavaScript/TypeScript code.
